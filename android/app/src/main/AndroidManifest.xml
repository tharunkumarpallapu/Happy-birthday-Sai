<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Notification permissions (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Alarm scheduling permissions -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    
    <!-- Boot completed permission -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Wake lock permission -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:label="@string/app_name">
        <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Notification receiver for handling alarm broadcasts -->
        <receiver
            android:name="com.romancestory.app.NotificationReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="GOOD_MORNING" />
                <action android:name="BREAKFAST" />
                <action android:name="LUNCH" />
                <action android:name="MOOD_CHECK" />
                <action android:name="FAMILY_CARE" />
                <action android:name="DINNER" />
                <action android:name="GOOD_NIGHT" />
                <action android:name="SCREEN_TIME" />
                <action android:name="ADVANCE_BIRTHDAY" />
                <action android:name="BIRTHDAY" />
                <action android:name="TEST_RANDOM" />
            </intent-filter>
        </receiver>

        <!-- Boot receiver for rescheduling alarms on device restart -->
        <receiver
            android:name="com.romancestory.app.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
